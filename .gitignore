# OS
.DS_Store

# Editors
*.swp
*.swo
*~
.vscode/
.idea/

# Python
__pycache__/
*.pyc
.venv/
.mypy_cache/

# Build artifacts
*.o
*.so

# MP-SPDZ (if cloned as subdir)
MP-SPDZ/

# python-circil (if cloned as subdir)
python-circil/

# Per-run artifacts (.bc/.sch, party stdout/stderr, report.json)
runs/

# Claude Code runtime state (only .claude/agents/ is tracked)
.claude/scheduled_tasks.lock
.claude/worktrees/

# Local-only Claude Code notes and per-task briefings (copied into each
# worktree via .worktreeinclude). Not for git history.
CLAUDE.local.md
briefings/
